export type themeProps = {
    dark: boolean;
    colors: {
        primary: string;
        background: string;
        card: string;
        text: string;
        error: string;
        success: string;
    };
};
